"use strict";

export const DEFAULT_PORT = 3000;
export const DEFAULT_HELP = false;
export const DEFAULT_QUIETLY = false;
export const DEFAULT_VERSION = false;
export const DEFAULT_WATCH_PATTERN = null;
export const DEFAULT_ALLOWED_ORIGIN = null;

export default {
  DEFAULT_PORT,
  DEFAULT_HELP,
  DEFAULT_QUIETLY,
  DEFAULT_VERSION,
  DEFAULT_WATCH_PATTERN,
  DEFAULT_ALLOWED_ORIGIN
};
