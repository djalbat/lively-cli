'use strict';

const LIVELY_CLI = 'lively-cli',
      DEFAULT_PORT = 3000,
      SWIRLY_INTERVAL = 250,
      PACKAGE_JSON_FILE_NAME = 'package.json';

module.exports = {
  LIVELY_CLI,
  DEFAULT_PORT,
  SWIRLY_INTERVAL,
  PACKAGE_JSON_FILE_NAME
};
