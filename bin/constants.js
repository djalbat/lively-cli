"use strict";

const WILDCARD = "*",
      LIVELY_CLI = "lively-cli",
      OPTIONS_GET = "OPTIONS, GET",
      PACKAGE_JSON = "package.json";

module.exports = {
  WILDCARD,
  LIVELY_CLI,
  OPTIONS_GET,
  PACKAGE_JSON
};
